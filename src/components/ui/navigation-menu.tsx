'use client';

import { navigationMenuTriggerStyle } from "./navigation-menu-variants";

export function NavigationMenu() {
  return (
    <nav className={navigationMenuTriggerStyle()}>
      {/* Navigation menu content */}
    </nav>
  );
}
